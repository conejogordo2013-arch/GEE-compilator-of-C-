import system;
extern int32 str_eq(string, string);
extern int32 str_len(string);
int32 main() {
    if (str_eq("ok", "ok") && str_len("tooling") == 7) {
        system_exit(0);
    }
    system_exit(1);
    return 0;
}
